import { Box, Typography } from "@mui/material";

const SchoolSettings = () => {
    return (
        <Box>
            <Typography variant="h6">School Settings</Typography>
            <Typography variant="body2">
                Update school name, address and other details.
            </Typography>
        </Box>
    );
};

export default SchoolSettings;